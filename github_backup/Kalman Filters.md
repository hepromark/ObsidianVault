Why do we even need to linearize non-linear dynamics?
- because there is no closed form solution for P_k+1 in the general case
- We can only compute the next P_k+1 analytically when the state transition rule is linear (a matrix multiplication)
![[Pasted image 20251126155739.png]]
